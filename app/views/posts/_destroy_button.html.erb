<% if policy(post).destroy? %>
  <%= button_to(
    t("destroy"),
    post,
    method: :delete,
    class: "button-danger",
    form: { data: { turbo_confirm: t("destroy_confirm") } }
  ) %>
<% end %>
