<%= simple_form_for users_setting do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <%= f.association :user %>
  <%= f.input :editor_type %>

  <%= f.button :submit %>
<% end %>
